# 将棋の対局動画を棋譜にする

**いまは第0段。「盤あわせ」だけができている。** 駒はまだ読んでいない。

姉妹アプリに [kifu](https://github.com/kakugawari/kifu)（手で棋譜を書くアプリ）がある。
ルールと KIF 書き出しはあちらから持ってくる。色の決めごともそろえてある。

---

## 何を作っているか

子供の対局をアームで真上から撮り、その動画から**消費時間つきの KIF** を起こす。
検討はぴよ将棋などの外部ソフトに投げる。**思考エンジンは積まない。**

### 決めごと（相談して決めたこと。勝手に変えない）

| | |
|---|---|
| 形態 | PWA。素の HTML / CSS / JS。ビルドを挟まない |
| 入り口 | **録った動画のファイルから**。カメラのライブ記録は後まわし |
| 撮り方 | アームで**真上**、iPhone **縦**、標準レンズ、1080p、**盤だけ**を横幅いっぱいに |
| 向きの指定 | 四隅（画面の左上から時計回り）＋ **先手は 手前／奥 の2択**。左右は無い |
| 認識の方針 | **駒の字を読まない。**フレーム間の差分を、合法手と照らして1手に絞る |
| 出口 | 消費時間つき KIF |
| 作らない | 形勢バー・戦型判定・QR・外部カメラ・課金・アカウント |

### なぜ動画ファイルからなのか

リアルタイムにすると、速指しへの追従・発熱・電池・画面が消える、が全部のしかかる。
動画なら**同じコマで何度でも試せる**ので、失敗した対局が回帰テストになる。
リアルタイムは、できあがった認識にライブのコマを流しこむだけ。順番を逆にすると作り直しになる。

### なぜ駒の字を読まないのか

読むと、書体ごとの学習データが要る。市販アプリが「特殊な書体は認識しない場合があります」と
断っているのはそのため。**こちらは自分の盤ひと組しか相手にしない。**
対局開始のコマには 40 枚の駒が既知の配置で並んでいるので、そのコマが
「その日の・その照明での見本」になる。学習が要らない。

読まない代わりに手放すもの → **打った駒の種類**（3択で聞く）、**途中局面からの開始**、
**見失ったときの自動復帰**（動画を巻き戻して人が直す）。

### 道のり

| 段 | 中身 | いま |
|---|---|---|
| 0 | 動画/写真を選ぶ → 四隅を合わせる → まっすぐな盤を出す → **撮り方を点検する** | ✅ できた |
| 1 | 9×9 の占有マップ（駒の有無・向き・成り）を盤に重ねて出す | |
| 2 | コマ間の差分 → 合法手と照合 → 1手に決める。**当たった手数を数字で出す** | |
| 3 | 迷ったら3択で聞く。手で直せるようにする | |
| 4 | 消費時間つき KIF を書き出す | |

**第2段の数字が出た時点で、この方式でいけるかの答えが出る。** そこまでが勝負。

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # 算数のテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
```

`npm run test:ui` は本物のブラウザに**作った盤の写真を流しこんで**、指の操作を再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

## 構成

```
index.html / styles.css   画面
core.js                   算数。DOM を触らない。ブラウザと node の両方で動く
app.js                    操作・描画
core.test.js              算数のテスト (node --test)
browser-test.js           ブラウザで動かすテスト
serve.js                  開発用サーバー
sw.js                     オフライン用。**通信優先**（キャッシュ優先にしない）
manifest.webmanifest      ホーム画面に追加したときの名前とアイコン
icon.svg                  タブのアイコン
icon-*.png                ホーム画面のアイコン
apple-touch-icon.png      iOS のホーム画面用 (SVG は使えないので PNG)
tools/make-icons.js       アイコンを作りなおす (Playwright が要る)
tools/bundle.js           全部を1枚の HTML にまとめる (テストプレイ用)
```

ファイルはすべて直下に置く。スマホやタブレットからでも1ファイルずつ差し替えられるようにするため。

`window.__app` はテストから中身をのぞくための入口。

### core.js にあるもの

| | |
|---|---|
| `homography(quad)` | 単位正方形を四角形へ写す変換。`g` `h` が 0 に近いほど真上 |
| `orientQuad(corners, sente)` | 画面の四隅 → 盤の座標。`'near'` = 先手が手前、`'far'` = 奥 |
| `boardH` / `cellCenter` / `cellQuad` | 盤の座標 → 画像の座標 |
| `shotMetrics` / `judge` | 撮り方を数字にして、良し悪しを付ける |
| `rectify` | 歪んだ画像をまっすぐに直す |

盤の座標は kifu とそろえてある。**`x=0` が9筋、`y=0` が一段目**（先手から見て奥）。

### sw.js は通信優先。kifu からそのまま持ってこない

**kifu の `sw.js` はキャッシュ優先** (`return hit||net`) で、直したものが出るのが
次のリロードになる。落とし穴の表にある「直したのに古い画面が出る」は、まさにこれ。
こちらは**通信を先に試し、3秒で返らないときだけキャッシュに落ちる**ようにしてある。
`browser-test.js` の「直したものが 1 回のリロードで出る」がその見張り。

---

## 確かめていないこと（正直に）

- **実機の iPhone で撮った動画は、一度も通していない。** 合成した画像でしか試していない
- **縦で撮った動画の向き。** iPhone の縦動画は「横のまま＋回して出せ」という
  情報つきで入っていることがある。`<video>` は正しく出しても `canvas` に写すと
  向きが食い違う、という事故が知られている。だから画面に**必ず幅×高さと縦横を出す**ようにした。
  実機の動画で最初に確かめる項目
- レンズの歪み。射影変換は**直線を直線にしか写せない**ので、樽型の歪みは直せない。
  盤の 1.5〜2 倍の高さを取り、超広角(0.5x)を使わないことで避ける。補正は入れていない

---

## 作り方（毎回これをなぞる）

### 1. 土台を決める

- **正しさを仕組みで保証できないか、先に考える。**「あとで検算する」より
  「そもそも間違いようがない作り」の方が強い
  （例: 駒の字を読むかわりに、合法手と照らして絞る）
- **ロジックを画面から切り離す。** `core.js` は DOM を触らない。だから node でテストできる
- **ビルドを挟まない。** 動かすまでの手数が少ないほど、確かめる回数が増える

### 2. 確かめる

- **「テストが通った」で終わらせない。実際に動かして目で見る。**
  画面の不具合はユニットテストをすり抜ける
- **体感の問題は計測する。** 憶測で直さない（いまは「直すのにかかった時間」を画面に出している）
- **端やずらした条件でも試す。** 真四角や原点だけで試すと、ずれていても差が出ず見逃す。
  射影変換のテストは**必ず台形で**行う
- **直したら見張りのテストを足す。** 同じ失敗を二度しないための唯一の方法
- **スマホの幅で見る。** 遅い端末でも計測する

### 3. テストプレイしてもらう

1. 全ファイルを 1 枚の HTML にまとめる（CSS も JS も埋め込む）
2. Artifact として公開し、リンクを渡す。スマホでもすぐ開ける
3. **そのリンクは Claude 側の仮ページ。** アドレスもタブのアイコンも Claude のものになる。
   本番のアドレスとは別だと必ず伝える
4. 直したら同じリンクを更新する（新しいリンクを作らない）

### 4. git と公開

- **作業用ブランチで進める。main へは、言われてから出す**
- コミットは「何を、なぜ」。直した不具合は**原因**まで書く。日本語でよい
- **push する前に `npm test` と `npm run test:ui` を両方流す**
- マージやデプロイの前に、**実際の設定を確認してから話す**

### 5. 報告のしかた

- 直したことは事実で書く。**計測値や試した件数を添える**
- できていないこと、飛ばしたことは、そのまま言う
- 間違えたら、言い訳せずその場で訂正して先へ進む

---

## 踏んだ落とし穴（同じ轍を踏まないために）

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 四隅を同じ点に重ねても、変換ができてしまう | 平行四辺形の分岐で、つぶれているかを見ていなかった | 行列式が 0 かどうかで弾く |
| 手順の番号が 1・3・4・5 と飛ぶ | 途中の手順を隠しても、番号が HTML に直書きのままだった | 出ている手順だけに、上から振りなおす |
| 四隅を直しても、結果が見えない | 縦の写真を幅いっぱいに出すと、画面 2 枚ぶんの高さになる | 高さ (52vh) で頭を打たせる |
| 四隅を動かすたびに固まる | 指が動くたびに盤ぜんぶを作り直していた | 動かしている間は枠だけ。**指を離してから**直す |
| 合わせる前から「ぴったり真上・0.0°」と満点が出る | はじめの四隅を真四角に置いているので、点検が真四角を測っていた。**点検する道具が、点検する前に合格を出していた** | 一度も動かしていないうちは点検の結果を出さない (`state.touched`) |
| 直した盤の四隅のマス名が読めない | マスの真ん中に書いていた。**初期配置では四隅に必ず香がいる**ので、いつも駒の下になる。向きを確かめるための表示なのに、確かめられない | 盤の角へ寄せて、地色の札に載せる |
| アイコンの四隅の点が切れる | 角丸のタイルは、角の方向に食い込む (半径22.5%なら中心から 0.614S)。盤を大きくすると、その角に置いた点がはみ出す | 点の外周が 0.58S（maskable は安全圏の 0.40S）に収まる大きさに盤を決める |

kifu で踏んだもの（画面まわりで効くもの）:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 置いた直後に要素が一瞬とぶ | CSS の `transform: scale` が位置決めの `translate` を上書きしていた | 位置は `left`/`top` で決める |
| アニメーションが 1 フレームも動かない | 同じフレーム内で書き換えると、途中の状態が見られない | `element.animate()` を使う |
| ホーム画面のアイコンが違うものになる | iOS は `apple-touch-icon` に SVG を使えない | PNG を用意する |
| 直したのに古い画面が出る | サービスワーカーがキャッシュ優先だった | つながっていれば最新を取りにいく |
| タップしても反応しない | 上に重ねた層が全面をふさいでいた | 反応させたい所だけ `pointer-events` を有効にする |

**新しく踏んだら、この表に足すこと。**
